# Include directories
include_directories(${CMAKE_SOURCE_DIR})

set(CMAKE_CXX_STANDARD 17)

# List source files
set(SOURCES
    neuralnetwork.cpp
    # Add other source files if needed
)

# Create executable or library
add_executable(neuralnetwork_executable ${SOURCES})

add_subdirectory(Chess_White)
add_subdirectory(Chess_White_2)
add_subdirectory(CSVFiles)
add_subdirectory(NetworkTest)
add_subdirectory(Scalers)
